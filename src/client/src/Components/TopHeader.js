import { Layout } from "antd";

const { Header } = Layout;

export const TopHeader = () => (
  <Header
    className="site-layout-sub-header-background"
    style={{
      padding: 0,
      height: "10vh",
    }}
  />
);
